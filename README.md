# Как запустить проект

### Шаг 1. Создание виртуального окружения
Нужно создать изолированную среду:
```bash
python -m venv venv
```
### Шаг 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 3. Настройка файла .env (Секреты)
1. Измени **`.env.example`** на **`.env`**
2. Укажи в этом файле свои значения от локального PostgreSQL:

### Шаг 4. Создание базы данных
На своем компьютере и создайте пустую базу данных с именем **`studguide`** (или другой, который ты указал в **`.env`**).

### Шаг 5. Применение миграций
```bash
python manage.py migrate
```

### Шаг 6. Создание администратора
Чтобы ты мог зайти в админ-панель и создавать статьи, создай своего локального суперпользователя:
```bash
python manage.py createsuperuser
```

### Шаг 7. Запуск проекта!
```bash
python manage.py runserver
```
